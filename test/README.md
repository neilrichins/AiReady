# Tests

Tests use the built-in Node.js test runner and create disposable fixtures in the operating system temporary directory.

```bash
npm test
```

Coverage includes safe non-overwriting initialisation, required structural controls, unresolved placeholders, missing targets, internal Markdown links, generic provenance leaks, and this framework's own contract.

These tests do not establish that an adopting project's policies are accurate or effective.
